[package]
name = "tgraphy-core"
version = "0.0.1"
edition = "2024"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
jsonschema = "0.26"
thiserror = "1"

[dev-dependencies]
cargo-llvm-cov = "0.8.7"
